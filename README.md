# spring-proxy-docker

docker compose up -- to start the application.
URL - "http://localhost:8080/backend1/hello" to connect to backend spring boot server1.
URL - "http://localhost:8080/backend2/hello" to connect to backend spring boot server2.
